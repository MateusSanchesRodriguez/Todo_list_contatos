class Contatos {
  nome: string
  email: string
  tel: string
  id: number

  constructor(nome: string, email: string, tel: string, id: number) {
    this.nome = nome
    this.email = email
    this.tel = tel
    this.id = id
  }
}

export default Contatos
